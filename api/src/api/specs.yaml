openapi: 3.0.1
info:
  title: Swagger Petstore
  description:
    'This is a sample server Petstore server.  You can find out more about     Swagger
    at [http://swagger.io](http://swagger.io) or on [irc.freenode.net, #swagger](http://swagger.io/irc/).      For
    this sample, you can use the api key `special-key` to test the authorization     filters.'
  contact:
    email: apiteam@swagger.io
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
externalDocs:
  description: Find out more about Swagger
  url: http://swagger.io
paths:
  /:
    get:
      parameters:
        - in: query
          name: ids
          schema:
            type: string
      operationId: hello
      responses:
        200:
          description: success
  /artist/{name}:
    get:
      responses:
        200:
          description: success
  /artist/{name}/sculpts:
    get:
      responses:
        200:
          description: success
  /artist/{name}/sculpt/{sculptname}:
    get:
      responses:
        200:
          description: success
  /artist/{name}/sculpt/{sculptname}/colorways:
    get:
      responses:
        200:
          description: success
  /artist/{name}/sculpt/{sculptname}/colorway/{cwname}:
    get:
      responses:
        200:
          description: success
  /user/{name}:
    get:
      responses:
        200:
          description: success
  /collection/{username}:
    get:
      responses:
        200:
          description: success
  /wantlist/{username}:
    get:
      responses:
        200:
          description: success
